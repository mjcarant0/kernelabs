/* Central configuration for the Processes topic */

import { TopicConfig } from "../shared/types";

export const processesConfig: TopicConfig = {
  id: "processes",
  name: "Processes",
  description:
    "Understand what processes are and how the operating system manages them. Learn about process states, scheduling queues, schedulers, context switching, and key process operations.",
  shortDescription: "Process Management in Operating Systems",
};
